server:
  port: 9003

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848   # 配置服务注册中心Nacos的地址

management:
  endpoints:
    web:
      exposure:
        include: '*'





